<%= form_with(model: deal_message, id: "deal_message_form") do |form| %>
  <% if deal_message.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(deal_message.errors.count, "error") %> prohibited this deal_message from being saved:</h2>

      <ul>
        <% deal_message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :content %>
    <%= form.rich_text_area :content, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= form.text_field :deal_investor_id, class: "form-control", hidden: true  %>
  </div>

  <div class="form-group">
    <%= form.submit "Save", {class: "btn btn-primary"}%>
  </div>
<% end %>
