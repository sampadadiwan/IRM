<%= form_with(model: doc_visibility) do |form| %>
  <% if doc_visibility.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(doc_visibility.errors.count, "error") %> prohibited this doc_visibility from being saved:</h2>

      <ul>
        <% doc_visibility.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :document %>
    <%= form.number_field :document_id, class: "form-control", hidden: true  %>
    <%= form.text_field :document_name, class: "form-control", disabled: true, value: doc_visibility.document.name  %>
  </div>

  <div class="form-group">
    <%= form.label :visiblity_type %>
    <%= form.select :visiblity_type, DocVisibility::VISIBILITY, {}, {class: "form-control"} %>
  </div>

  <div class="form-group">
    <%= form.label :to %>
    <%= form.text_field :to, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= form.submit "Save", {class: "btn btn-primary"}%>
  </div>
<% end %>
